<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JKT48 Member Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #121212; color: white; font-family: 'Segoe UI', sans-serif; }
        .jkt-red { color: #df1f26; }
        
        .member-card { 
            background: #1e1e1e; 
            border: none; 
            border-radius: 12px; 
            overflow: hidden; 
            transition: 0.3s; 
            max-width: 260px;
            margin: 0 auto;
        }
        
        .member-card:hover { 
            transform: translateY(-8px); 
            box-shadow: 0 10px 25px rgba(223, 31, 38, 0.4); 
        }
        
        .member-img { 
            height: 320px; 
            object-fit: cover; 
            width: 100%; 
            border-bottom: 4px solid #df1f26; 
        }
        
        .name-badge { background: #df1f26; padding: 3px 12px; border-radius: 15px; font-size: 0.75rem; font-weight: bold; color: white; }
        .card-body { padding: 18px 15px; }

        /* PERBAIKAN: Warna Nama Lengkap agar Terlihat Jelas */
        .member-name { 
            font-size: 1.15rem; 
            font-weight: 700; 
            margin-bottom: 8px; 
            color: #ffffff !important; /* Putih murni */
            text-shadow: 1px 1px 3px rgba(0,0,0,0.8); /* Bayangan teks agar lebih tajam */
        }
        
        /* PERBAIKAN: Warna Info Tanggal Lahir */
        .info-text { 
            font-size: 0.85rem; 
            color: #eeeeee; /* Abu-abu sangat terang mendekati putih */
            margin-bottom: 15px; 
        }

        .social-link { color: #ffffff; font-size: 1.2rem; transition: 0.2s; opacity: 0.8; }
        .social-link:hover { color: #df1f26; opacity: 1; transform: scale(1.1); }
        
        .text-muted-custom { color: #aaaaaa; font-size: 0.9rem; }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="text-center mb-5">
            <h2 class="fw-bold"><span class="jkt-red">JKT48</span> MEMBER GALLERY</h2>
            <p class="text-muted-custom small">Visualisasi Data Real-time dari Database Member</p>
        </div>

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
            <% members.forEach(m => { %>
                <div class="col">
                    <div class="card member-card h-100 shadow">
                        <img src="<%= m.foto_url %>" class="member-img" alt="<%= m.nama_lengkap %>">
                        <div class="card-body text-center">
                            <div class="member-name"><%= m.nama_lengkap %></div>
                            
                            <div class="d-flex justify-content-center mb-3">
                                <span class="name-badge text-uppercase"><%= m.nama_panggilan %></span>
                            </div>
                            
                            <div class="info-text">
                                <i class="fas fa-calendar-alt me-1 jkt-red"></i>
                                <%= new Date(m.tanggal_lahir).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %>
                            </div>

                            <div class="d-flex justify-content-center gap-4">
                                <% if(m.instagram) { %>
                                    <a href="https://instagram.com/<%= m.instagram %>" target="_blank" class="social-link" title="Instagram">
                                        <i class="fab fa-instagram"></i>
                                    </a>
                                <% } %>
                                <% if(m.tiktok) { %>
                                    <a href="https://tiktok.com/@<%= m.tiktok %>" target="_blank" class="social-link" title="TikTok">
                                        <i class="fab fa-tiktok"></i>
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="text-center mt-5">
            <a href="/dashboard" class="btn btn-outline-danger rounded-pill px-5 shadow-sm">
                <i class="fas fa-arrow-left me-2"></i>Kembali ke Dashboard
            </a>
        </div>
    </div>
</body>
</html>