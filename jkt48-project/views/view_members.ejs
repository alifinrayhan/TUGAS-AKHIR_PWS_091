<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JKT48 Member Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #121212; color: white; font-family: 'Segoe UI', sans-serif; }
        .jkt-red { color: #df1f26; }
        
        /* Mengatur ukuran kartu agar tidak kegedean */
        .member-card { 
            background: #1e1e1e; 
            border: none; 
            border-radius: 12px; 
            overflow: hidden; 
            transition: 0.3s; 
            max-width: 260px; /* Batas lebar maksimal kartu */
            margin: 0 auto;
        }
        
        .member-card:hover { transform: translateY(-8px); box-shadow: 0 10px 20px rgba(223, 31, 38, 0.3); }
        
        /* Mengatur ukuran gambar agar proporsional */
        .member-img { 
            height: 320px; /* Tinggi gambar dibatasi */
            object-fit: cover; 
            width: 100%; 
            border-bottom: 3px solid #df1f26; 
        }
        
        .name-badge { background: #df1f26; padding: 3px 12px; border-radius: 15px; font-size: 0.75rem; font-weight: bold; }
        .card-body { padding: 15px; }
        .member-name { font-size: 1.1rem; font-weight: bold; margin-bottom: 5px; }
        .info-text { font-size: 0.8rem; color: #aaa; margin-bottom: 10px; }
        .social-link { color: white; font-size: 1.1rem; transition: 0.2s; }
        .social-link:hover { color: #df1f26; }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="text-center mb-5">
            <h2 class="fw-bold"><span class="jkt-red">JKT48</span> MEMBER GALLERY</h2>
            <p class="text-muted small">Daftar Member JKT48 Real-time</p>
        </div>

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
            <% members.forEach(m => { %>
                <div class="col">
                    <div class="card member-card h-100 shadow">
                        <img src="<%= m.foto_url %>" class="member-img" alt="<%= m.nama_lengkap %>">
                        <div class="card-body text-center">
                            <div class="member-name"><%= m.nama_lengkap %></div>
                            <div class="d-flex justify-content-center mb-2">
                                <span class="name-badge text-uppercase"><%= m.nama_panggilan %></span>
                            </div>
                            
                            <div class="info-text">
                                <i class="fas fa-calendar-alt me-1"></i>
                                <%= new Date(m.tanggal_lahir).toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                            </div>

                            <div class="d-flex justify-content-center gap-3">
                                <% if(m.instagram) { %>
                                    <a href="https://instagram.com/<%= m.instagram %>" target="_blank" class="social-link"><i class="fab fa-instagram"></i></a>
                                <% } %>
                                <% if(m.tiktok) { %>
                                    <a href="https://tiktok.com/@<%= m.tiktok %>" target="_blank" class="social-link"><i class="fab fa-tiktok"></i></a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="text-center mt-5">
            <a href="/dashboard" class="btn btn-outline-danger btn-sm rounded-pill px-4">
                <i class="fas fa-arrow-left me-2"></i>Dashboard
            </a>
        </div>
    </div>
</body>
</html>