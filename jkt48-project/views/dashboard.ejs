<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JKT48 Developer Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
        .navbar-brand { font-weight: bold; color: #ff0000 !important; }
        .card { border: none; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); transition: 0.3s; }
        .card:hover { transform: translateY(-5px); }
        .api-box { background: #1a1d20; color: #00ff00; padding: 15px; border-radius: 10px; font-family: monospace; }
        .btn-jkt48 { background-color: #df1f26; color: white; border-radius: 8px; font-weight: bold; }
        .btn-jkt48:hover { background-color: #b1181d; color: white; }
        .admin-badge { background-color: #ff0000; color: #fff; font-size: 0.7rem; padding: 4px 8px; border-radius: 5px; }
        code { color: #df1f26; font-weight: bold; }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
        <div class="container">
            <a class="navbar-brand" href="/dashboard"><i class="fas fa-bolt me-2"></i>JKT48 <span class="text-dark">SERVICE</span></a>
            <div class="d-flex align-items-center">
                <span class="navbar-text me-3 d-none d-md-block">
                    Halo, <strong><%= user.username %></strong> 
                    <% if (user.role === 'admin') { %> <span class="badge admin-badge ms-1">ADMIN</span> <% } %>
                </span>
                <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill px-3">Keluar</a>
            </div>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="row justify-content-center">
            <div class="col-md-10">
                
                <% if (user.role === 'admin') { %>
                <div class="card mb-4 border-start border-danger border-5">
                    <div class="card-body p-4 d-flex justify-content-between align-items-center flex-wrap">
                        <div>
                            <h4 class="text-danger fw-bold mb-1"><i class="fas fa-user-shield me-2"></i>Mode Administrator</h4>
                            <p class="text-muted mb-0">Kelola database member dan sinkronisasi data.</p>
                        </div>
                        <a href="/admin" class="btn btn-danger px-4 py-2 shadow-sm"><i class="fas fa-cog me-2"></i>Panel Admin</a>
                    </div>
                </div>
                <% } %>

                <div class="row">
                    <div class="col-md-6 mb-4">
                        <div class="card h-100">
                            <div class="card-body p-4">
                                <h5 class="fw-bold mb-3"><i class="fas fa-code text-primary me-2"></i>Data Mentah (JSON)</h5>
                                <p class="small text-muted">Gunakan link ini untuk mengambil data dalam format JSON untuk aplikasi/coding.</p>
                                
                                <% if (user.api_key) { %>
                                    <div class="api-box mb-3 small">
                                        <code><%= user.api_key %></code>
                                    </div>
                                    <a href="/api/members?api_key=<%= user.api_key %>" target="_blank" class="btn btn-dark w-100 mb-2">
                                        <i class="fas fa-file-code me-2"></i>Buka JSON Mentah
                                    </a>
                                <% } else { %>
                                    <div class="alert alert-warning small">API Key belum dibuat.</div>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-6 mb-4">
                        <div class="card h-100 border-top border-primary border-4">
                            <div class="card-body p-4 text-center">
                                <h5 class="fw-bold mb-3 text-primary"><i class="fas fa-eye me-2"></i>Visual Preview</h5>
                                <p class="small text-muted">Lihat bagaimana data kamu tampil dalam bentuk Galeri yang cantik.</p>
                                
                                <% if (user.api_key) { %>
                                    <div class="py-3">
                                        <i class="fas fa-images fa-3x text-light-emphasis mb-3"></i>
                                    </div>
                                    <a href="/view/members?api_key=<%= user.api_key %>" target="_blank" class="btn btn-primary w-100 shadow-sm">
                                        <i class="fas fa-th-large me-2"></i>Buka Galeri Member
                                    </a>
                                <% } else { %>
                                    <button class="btn btn-secondary w-100" disabled>API Key Diperlukan</button>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="text-center mt-2">
                    <form action="/generate-key" method="POST">
                        <button type="submit" class="btn btn-jkt48 px-5 py-2 shadow">
                            <i class="fas fa-sync-alt me-2"></i><%= user.api_key ? 'Update / Ganti API Key' : 'Buat API Key Baru' %>
                        </button>
                    </form>
                </div>

            </div>
        </div>
    </div>
</body>
</html>